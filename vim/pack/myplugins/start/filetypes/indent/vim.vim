" VIMSCRIPT FORMATTING SETTINGS

" Define the tab as two spaces
setlocal shiftwidth=2 tabstop=2 softtabstop=0 expandtab smarttab

" Define the max number of characters per line to be 100
setlocal textwidth=100
